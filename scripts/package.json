{
  "name": "s3-uploader",
  "version": "1.0.0",
  "description": "S3 Presigned URL Uploader",
  "main": "generate-presigned-url.js",
  "scripts": {
    "generate-url": "node generate-presigned-url.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.x",
    "@aws-sdk/credential-provider-env": "^3.x",
    "@aws-sdk/credential-provider-ini": "^3.x",
    "@aws-sdk/credential-provider-node": "^3.x",
    "@aws-sdk/s3-request-presigner": "^3.x",
    "commander": "^11.0.0"
  }
}
